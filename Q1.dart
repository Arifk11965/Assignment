// 1.Create two integer variables length and breadth and assign values then check if they
// are square values or rectangle values. ie: if both values are equal then it's square
// otherwise rectangle.
//

void main() {
  int length = 10;
  int breadth = 25;
  if (length == breadth) {
    print("Selected Ranges Represent The shape is = square.");
  } else {
    print("Selected Ranges Represent The shape is = rectangle.");
  }
}
